# ========== API Configuration ==========
# PageIndex RESTful API Base URL (local development)
VITE_PAGEINDEX_API_URL=http://localhost:8003

# For Next.js deployment, use:
# NEXT_PUBLIC_PAGEINDEX_API_URL=http://localhost:8003

# ========== LLM Provider Configuration ==========
# SECURITY: These are backend-only keys. NEVER prefix them with VITE_
# (VITE_ prefixed variables are bundled into client JavaScript and publicly visible)
# These keys are only needed if running local API fallback
LLM_PROVIDER=gemini

# Optional: Specify model (uses provider default if not specified)
# LLM_MODEL=gemini-2.5-flash

# ========== Gemini Configuration ==========
GEMINI_API_KEY=your_gemini_api_key_here

# ========== DeepSeek Configuration ==========
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ========== OpenRouter Configuration ==========
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ========== OCR Service Configuration ==========
# OCR service URL (for document image recognition)
VITE_OCR_SERVICE_URL=http://localhost:8010

# ========== Development Configuration ==========
# VITE_API_BASE_URL=http://localhost:8000
